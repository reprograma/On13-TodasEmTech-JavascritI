// 1. crie uma array com objetos, cada objeto com pelo menos 4 propriedades
const lancheDaTarde = [
    { 
        doce: 'brigadeiro',
        salgado: 'avoador',
        azedo: 'laranja',
        refrescante: 'Suco'
    }

]

// 2. use métodos para acessar valores das propriedades desses objetos, e encontrar objetos pelo indice da lista.

lancheDaTarde[0].doce
console.log(lancheDaTarde[0].doce);

lancheDaTarde[0].azedo
console.log(lancheDaTarde[0].azedo);

/*
const comidas = [
    {
        moqueca: {
            quantidade: 0,
            caracteristica: 0,
        },
        pastel: {
            quantidade: 0,
            caracteristica: 0,
        }, 
        salada: {
            quantidade: 0,
            caracteristica: ["gostoso", "saúdavel"],
        },

    }
]

console.log(comidas[0].pastel.quantidade) */

/* let familia =[
    {
        nome: 'Maria',
        grauParentesco: 'mãe',
        peso: 70,
        altura: '150cm',
    },

    {
        nome: 'Hadassa',
        grauParentesco: 'filha',
        peso: 32,
        altura: '140',
    },
    {
nome: 'nicodemio',
grauParentesco: 'pai',
peso: 67,
altura: '175 cm',
    }
];
// 2. use métodos para acessar valores das propriedades desses objetos, e encontrar objetos pelo indice da lista.
console.log(familia[1].altura);
console.log(familia[0].grauParentesco);
console.log(familia.length);
//console.log(familia[0],[1],[2].nome);

for(let indice= 0; indice < familia.length; indice++){
    console.log(familia[indice].nome);
*/